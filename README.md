# ZEY — Specialty Coffee, Matcha & Brunch

A responsive static landing page for Coffee Zey, designed for Vercel deployment.

## Deploy
Import this GitHub repository into Vercel. No build command is required; the site is a static `index.html` project.

## Assets
Place the provided brand imagery in `assets/` using the filenames referenced by `index.html`.
